server:
  session:
    timeout: 60
  context-path: /
  port: 9000
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/springaboutme?characterEncoding=utf-8&useSSL=false
    username: root
    password: root
mybatis:
  type-aliases-package: com.aopLogLog.demo.aopLogdemo.dataobject
  mapper-locations: classpath:mapper/*.xml